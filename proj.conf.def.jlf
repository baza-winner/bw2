$portRange = %($bwTagDir)/data/conf.jlf|range
^{
  type Map
  keysDef {
    bw {
      type Map
      keysDef {
        tag String
      }
    }
    projPortIncrement {
      type Int
      range 0..($portRange|max - $portRange|min)
    }
    ports {
      type Map
      elemDef Map
    }
  }
}